// Тиждень 8: Layout для сторінок аутентифікації (login, register)
// Мінімальний layout без Header/Footer

export const metadata = {
  title: "Аутентифікація",
};

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
